# === Database ===
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=trading
POSTGRES_USER=trading
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here

# === Redis ===
REDIS_HOST=localhost
REDIS_PORT=6379

# === ByBit API (read-only keys!) ===
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=true

# === Telegram Bot ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === Sentiment APIs ===
# LunarCrush (optional)
LUNARCRUSH_API_KEY=
# Fear & Greed — no key needed

# === Whale Alert (optional) ===
WHALE_ALERT_API_KEY=

# === LLM API (for sentiment analysis) ===
LLM_API_KEY=
LLM_MODEL=claude-sonnet-4-20250514

# === OpenClaw Webhook (receives analysis from OpenClaw) ===
OPENCLAW_WEBHOOK_SECRET=

# === n8n Webhooks (push notifications — leave empty to disable) ===
# Format: http://n8n:5678/webhook/<webhook-id>
# Access n8n UI via SSH tunnel: ssh -L 5678:localhost:5678 user@vps
N8N_WEBHOOK_SIGNAL=
N8N_WEBHOOK_SCREENER=
N8N_WEBHOOK_LISTING=
# n8n basic auth credentials
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_ME

# === General ===
LOG_LEVEL=INFO
ENVIRONMENT=development
